# [Claude Code 작업 지시서] 글로벌 사업장 HDPS KPI 자동집계·미달성 메일링

- 기획자: 서지철 (HD건설기계, Lean 컨설팅 담당)
- 출처: 생성형 AI 업무자동화 전문가과정 [1차수] — 추가 기획서 4번 (2026-08-22 메일 전달)
- 프로젝트 유형: 웹 포털 (엑셀 임포트 + 지표 판정 + 자동 메일링)
- 대체 대상: 기획자가 직접 만든 **엑셀 VBA 매크로 3모듈** (`modImport` / `modCheck` / `modMail`)

---

## 0. Claude Code에게 — 작업 방식

1. 이 프로젝트는 **백지에서 시작하는 기획이 아니다.** 기획자가 이미 매크로로 한 번 만들었고,
   "매크로·파이썬만 활용했는데 원하는 대로 잘 안 나온다"고 한 상태다.
   따라서 **매크로가 실제로 걸려 넘어진 자리를 그대로 재현·검증하는 것**이 최우선이다.
   그 자리는 기획자가 남긴 `셀프운영_가이드.md`에 5개로 적혀 있다(부록 B).
2. **사내 실데이터를 저장소에 넣지 않는다.** 기획자가 "저만 관리하는 자료인데 반출할 수가 없어서
   교육당일 보여드려야 할 것 같다"고 명시했다. 지표명·모듈 체계 같은 **구조는 살리고,
   목표·실적 수치는 전부 생성기로 만든 더미**를 쓴다.
3. 계산은 전부 `js/logic.js`(UMD)에 모아 Node에서 단위 테스트한다.
   **판정 로직을 일부러 뒤집어 보고 테스트가 잡는지 확인**한 뒤에야 통과로 친다.
4. 부록 A의 원문이 최종 기준이다.

---

## 1. 한 줄 요약

글로벌 6개 사업장이 매달 보내오는 **KPI 원본 엑셀을 업로드하면 매핑표대로 자동 전사**되고,
목표 대비 **달성/미달성이 자동 판정**되며, 미달성 지표는 **담당팀별로 묶여 사유·만회대책 요청
메일 초안까지 자동 작성**되는 웹 포털.

## 2. 배경 및 현재 업무 상황

- 기획자는 글로벌 사업장의 HDPS(생산제조혁신활동) 성과지표를 총괄 관리한다.
- 사업장마다 KPI가 100여 개, 사업장이 6곳이므로 매달 600건 이상을 손으로 옮기고 판정한다.
- 현재는 엑셀 VBA 매크로로 자동화해 두었고, 매달 이 4단계를 반복한다.
  1. 인도/브라질에서 새 KPI 파일 받기
  2. `ImportOverseasData` 실행 (사업장별 1회) — 원본 → 총괄파일 전사
  3. `CheckAchievement` 실행 — 달성 초록/미달성 빨강 색칠 + "미달성목록" 탭 생성
  4. `SendUnderperformanceEmails` 실행 — 미달성 메일 검토 후 발송
- 매크로가 잘 안 되는 이유는 **로직이 아니라 원본 파일의 표기 흔들림**이다(부록 B).

## 3. 요구 기능

### 3.1 데이터 자동입력 (modImport 대체)
- 원본 KPI 엑셀(.xlsx/.xlsm) 업로드 → 시트 선택.
- **KPI 이름이 가장 많이 일치하는 열을 자동 탐지**한다.
  매크로처럼 행 번호(`원본행`, `원본 실적행`)를 박아 두지 않는다 — 원본 서식에서 행이 밀리면
  매크로는 엉뚱한 값을 읽지만, 이름으로 찾으면 밀려도 맞는다.
- 매핑표에 따라 **환산배율·값형식**을 적용해 숫자로 변환.
- 결과를 **전사 대상 / 건너뜀(사유별) / 매핑 누락**으로 나눠 보여주고, 확인 후 반영.
- 항상 읽히는 **표준 양식 엑셀**을 내려받을 수 있게 한다(원본 서식이 너무 다를 때의 탈출구).

### 3.2 달성 판정 (modCheck 대체)
- 기준월 선택 → 지표별 목표 vs 실적 비교.
- 판정방향 4종: **상향**(높을수록 좋음) / **하향**(낮을수록 좋음) / **목표일치** / **범위내**.
- 판정 결과 3종: **달성 / 미달성 / 판정불가**.
  - 실적이 아직 안 들어온 지표를 미달성으로 세면 자료가 늦는 달마다 달성률이 통째로 무너진다.
    **달성률의 분모는 판정 가능한 것만**으로 한다.
- 모듈별 달성률, 12개월 추이, 미달성 상위 지표를 대시보드에 표시.
- 판정 결과 + 미달성목록 2시트 엑셀 내보내기.

### 3.3 미달성 메일링 (modMail 대체)
- 미달성 지표를 **담당팀(R&R)별로 묶는다.** 총괄파일 R&R 칸에는 `생산팀\n혁신팀`처럼
  줄바꿈으로 여러 팀이 들어 있으므로 **나눠서 양쪽 모두에 넣는다.**
- 팀별 메일 초안 자동 작성: 지표명·목표·실적·차이·이탈률·판정기준 + 회신 항목 4가지
  (① 미달 사유 ② 만회대책 ③ 완료 목표일 ④ 담당자).
- 이메일이 등록된 팀은 **발송 준비**, 없으면 **이메일 미등록**으로 구분.
- 발송은 `mailto:` 로 메일 클라이언트를 열어 **사람이 확인 후 보낸다.**
  매크로의 `SEND_DIRECTLY = False`(검토 후 발송)가 기본이었던 것과 같은 취지다.
- 초안 전체를 엑셀로 내려받아 메일머지에 쓸 수 있게 한다.

### 3.4 매핑표 / 담당자 / 실행로그
- 매핑표: 원본 영문 지표명 ↔ 총괄 국문 지표명, 판정방향, 환산배율, 값형식. 검색 가능.
- 담당자: 사업장 × 팀별 담당자명·이메일 입력(입력 즉시 저장).
- 실행로그: 실행일시·작업구분·사업장·월·처리건수·실패건수.

## 4. 데이터 스키마

- **Site(사업장)**: `id`, `name`, `region`, `kpis[]`
- **KPI**: `id`, `module`, `moduleCode`, `nameKo`, `nameEn`, `unit`, `direction`,
  `target`, `owner`, `scale`, `format`, `actuals{ '1월': n, … }`
- **Mapping(매핑표)**: `siteId`, `kpiId`, `kpiEn`, `kpiKo`, `module`, `direction`,
  `scale`(환산배율), `format`(숫자/시간serial/퍼센트텍스트), `excluded`
- **Contact(담당자)**: `siteId`, `owner`(팀), `name`, `email`
- **Log(실행로그)**: `at`, `kind`, `siteId`, `month`, `processed`, `failed`, `note`

샘플: 사업장 6곳 × KPI 120개 × 12개월. HDPS 표준 19개 모듈 체계를 따르되 **수치는 전부 더미**.
생성기는 `scripts/gen-seed.py` — 시드를 바꿀 일이 생기면 산출물이 아니라 이 스크립트를 고친다.

## 5. 제약사항 및 가정

- **사내 실데이터 반입 금지** (0-2 참조). 저장소는 public이므로 더미만 둔다.
- 빌드 단계 없음 — 순수 HTML/CSS/JS. GitHub Pages에 그대로 올라간다.
- 데모 DB는 브라우저 `localStorage`. 사업장이 6곳뿐이라 서버 없이도 전 과정이 돈다.
- 메일은 `mailto:` 기반. SMTP 직접 발송은 브라우저에서 불가능하며,
  기획자의 매크로도 기본값이 "검토 후 발송"이었으므로 동작이 오히려 같다.
- 엑셀 읽기/쓰기는 SheetJS 로컬 동봉본(CDN 미사용 — 사내망에서도 열려야 한다).

## 6. 기술 스택

- 프론트: 순수 HTML/CSS/JS (프레임워크 없음)
- 엑셀: SheetJS `lib/xlsx.full.min.js` (로컬 동봉)
- 저장: localStorage → 운영 전환 시 `js/store.js` 의 `load/save/log` 만 교체
- 테스트: Node 내장만 사용 (`node test/logic.test.js`)

## 7. 산출물

1. 웹 앱 (대시보드 / 임포트 / 판정 / 메일링 / 매핑표 / 담당자 / 실행로그)
2. 순수 로직 모듈 + 단위 테스트
3. 더미 시드 + 생성기 스크립트
4. README (사용법, 실데이터 전환 방법, 매크로 대응표)

## 8. 구현 단계

- Phase 1: 로직(값 변환·판정·메일 초안) + 단위 테스트 + 더미 시드
- Phase 2: 대시보드 / 판정 화면
- Phase 3: 엑셀 임포트(열 자동 탐지·표준 양식)
- Phase 4: 메일링 / 매핑표 / 담당자 / 실행로그, 배포

## 9. 확인 필요 사항

1. 사업장은 정확히 몇 곳이며 이름은 무엇인가? (현재 더미 6곳: 울산·인천·군산·인도·브라질·유럽)
2. 목표값이 **연간 고정**인가, 월별로 다른가? (현재는 연간 고정으로 구현)
3. "범위내" 판정의 허용 오차 기준은? (현재 기본값 목표의 ±5%)
4. 미달성 메일 회신을 **시스템에서 받아 관리**할 필요가 있는가? (현재는 발송까지만)
5. 사내 메일 발송을 자동화하려면 Outlook/Exchange 연동이 필요한데, 사내 정책상 가능한가?
6. 지표별 담당자가 팀 단위가 아니라 개인 단위인 경우가 있는가?

---

## 부록 A. 기획서 원문 (2026-08-22 메일)

> 4. 추가기획서) 글로벌 사업장에 대한 HDPS KPI(성과지표) 매크로를 활용한 데이터 자동입력,
> 지표 갱신 자동반영, 성과 모니터링(달성 지표, 미달성 지표 - 미달성 지표는 해당 공정에
> 사유 및 만회대책 요청 자동 메일링 구현)
>
> - 해당 파일은 제 전용 파일이나, 사내 보안으로 교육당일 공유 가능합니다.
>   파일이 아니더라도, 샘플 캡쳐하여 교육 전 공유는 드리겠습니다.

보충 (2026-08-21 카카오톡):

> 글로벌 6개공장(각 공장별 100여개 KPI매월 자동집계 프로그램 및 지표업데이트 갱신 관리,
> 지표 실적 달성/미달성 자동관리 및 미달성 지표 자동 메일링 윕페이지 개발) 하나 남았습니다.
> 이 남은 과제를 클로우드 매크로 이것저것 믹스해서 뭔가 만들어내야 할거 같습니다.
> 저만 관리하는 자료인데 반출할수가 없어서 교육당일 보여드려야 할거 같아요.

---

## 부록 B. 매크로가 걸려 넘어진 자리 (기획자의 `셀프운영_가이드.md` v3)

이 5개가 이 프로젝트의 진짜 요구사항이다. 웹으로 옮기면서 전부 테스트로 고정했다.

1. **스케일 변환 버그** — 원본은 `0.96`(소수)인데 총괄은 `96`(정수)으로 적는 관례라
   값이 100배 차이 났다. 매핑표 **L열(환산배율)** 을 곱해 해결.
   → 단, 원본에 `"96%"`처럼 이미 퍼센트로 적힌 값이 섞여 오면 100을 또 곱해 `9600`이 된다.
     웹판은 이 경우를 따로 잡는다(테스트 있음).
2. **시간값 처리** — MTTR/MTBF/MTTD가 `"00:48:00"` 형식. 매핑표 **M열(값형식)** = `시간serial`
   이면 시간 단위 숫자로 변환.
3. **퍼센트 텍스트** — `"+8%"` 같은 텍스트. M열 = `퍼센트텍스트`.
4. **타입 오류(Type Mismatch)** — `"제외"`, `"확인필요"`, `"TBD"` 같은 텍스트를 숫자와 비교하다
   매크로가 죽었다. → 웹판은 숫자로 해석되지 않는 값을 **사유와 함께 null로 걸러 낸다.**
5. **파일 중복 열림** — 대상 파일이 다른 창에서 열려 있으면 오작동.
   → 웹판에는 해당 없음(파일을 잠그지 않는다). 매크로를 웹으로 옮기는 이유 중 하나.

매핑표 확정 현황 (기획자 검토 완료): 인도 104개 중 **77 확정 / 27 제외**,
브라질 80개 중 **76 확정 / 4 제외**. 제외는 "현지(공장) 자체관리 지표로 총괄파일 미반영".

## 부록 C. 매크로 ↔ 웹 대응표

| VBA 매크로 | 웹 화면 | 로직 함수 |
|---|---|---|
| `modImport.ImportOverseasData` | ① 데이터 자동입력 | `Logic.convertValue` / `Logic.applyMapping` |
| `modCheck.CheckAchievement` | ② 달성 판정 | `Logic.judge` / `Logic.evaluateMonth` |
| `modMail.SendUnderperformanceEmails` | ③ 미달성 메일링 | `Logic.groupByOwner` / `Logic.buildMailDrafts` |
| 설정워크북 `매핑_인도` / `매핑_브라질` 탭 | 매핑표 | — |
| 설정워크북 `담당자메일` 탭 | 담당자 | — |
| 설정워크북 `실행로그` 탭 | 실행로그 | `Store.log` |
| 총괄파일 셀 색칠(초록/빨강) | 판정 표의 행 배경 | — |
| 설정워크북 `미달성목록` 탭 | 판정 결과 엑셀의 2번째 시트 | `Logic.collectUnderperformance` |
